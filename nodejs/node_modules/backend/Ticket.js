Ticket = function (data) {
    var _that = this;
    
    var ticketID = data.ticketID;
    var ticketTitle = data.ticketTitle;
    var ticketDescription = data.ticketDescription;
    var ticketType = data.ticketType;
    var ticketPriority = data.ticketPriority;
    var ticketDueDate = data.ticketDueDate;
    var ticketProjektID = data.ticketProjektID;
    var ticketUserID = data.ticketUserID;
    var ticketCreatedBy = data.ticketCreatedBy;
    var ticketCreationDate = data.ticketCreationDate;
    
    var ticketInProgress = data.ticketInProgress;
    
    this.getTicketID = function () {
        if (ticketID && ticketID != 0) {
            return ticketID;
        }
        else {
            return 'error ticketID undefined or null';
        }
    };
};

//check if nodejs is running this code
try {
    module.exports = function () {
        return Ticket;
    }();
} catch (e) {

}