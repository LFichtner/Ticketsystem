Filter = function (data) {
    var _that = this;
    
    var filterID = data.filterID;
    var filterValue = data.filterValue;
    var filterFunction = data.filterFunction;
    var filterFieldName = data.filterFieldName;
    
    this.getFilterID = function () {
        if (filterID && filterID != 0) {
            return filterID;
        }
        else {
            return 'F: ERROR filterID undefined or null';
        }
    };
};

//check if nodejs is running this code
try {
    module.exports = function () {
        return Filter;
    }();
} catch (e) {

}