1. init.js starts the server instance
2. in package.json are all importend documents listed and could be installed easily with "npm install"
dependencys can be add to package.json on install with "--save"
3. all modules and the backend are located in node_modules
4. Backend_Testroutine and serversite script to init Test are locatet in backend/tests